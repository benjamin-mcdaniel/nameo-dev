{
  "name": "nameo-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev":        "wrangler dev src/index.js --local",
    "deploy":     "wrangler deploy",
    "test":       "vitest run",
    "test:watch": "vitest",
    "test:qa":    "vitest run --config vitest.qa.config.js",
    "deploy:full": "npm test && wrangler deploy"
  },
  "dependencies": {
    "leo-profanity": "^1.7.0",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
