{
  "name": "nameo-collector",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config ../wrangler.collector.toml",
    "deploy": "wrangler deploy --config ../wrangler.collector.toml"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "wrangler": "^3"
  }
}
